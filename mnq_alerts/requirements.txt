# No databento SDK needed — we call the REST API directly with requests.
# This avoids the databento-dbn Rust extension which has no Python 3.8 wheels.
requests>=2.31.0
python-dotenv>=1.0.0
pytz>=2024.1
pandas>=2.0.0
numpy>=1.20.3

# Python 3.8 compatibility pin:
# multitasking 0.0.12 uses type[Thread] syntax which requires Python 3.9+
multitasking==0.0.11
